#include <bits/stdc++.h>
using namespace std ;

int main()
{
    // assign(n , x) : first n values to x , O(n)
    // push_back(x) : adds x to right , O(1)
    // pop_back() : removes the last element , O(1)
    // back() : value of the last element , O(1)
    // size() , O(1)
    // sort(v.begin() , v.end())
    // find(v.begin() , v.end() , x) - v.begin() -> first position of x in v
    // erase(it) : remove element at index it            O(n)
    // erase(it1 , it2) : remove elements in [it1 , it2)
    // insert(it , x) : insert element x before it
}
